# DraggableTree Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** @dnd-kit を用いた汎用ツリーD&Dコンポーネントを作成し、テストページで動作確認する。

**Architecture:** parentId ベースのフラットリストをツリー表示し、@dnd-kit の useDraggable/useDroppable でドラッグ&ドロップを実現する。ドロップ位置はノード高さの3分割（上1/4=兄弟前、中央2/4=子、下1/4=兄弟後）で判定。コールバック型APIでストア非依存。

**Tech Stack:** @dnd-kit/core, @dnd-kit/utilities, React, TypeScript, Tailwind CSS

**Design doc:** `docs/plans/2026-03-03-dnd-tree-design.md`

---

### Task 1: Install @dnd-kit + Create types and utils

**Files:**
- Create: `src/components/common/DraggableTree/types.ts`
- Create: `src/components/common/DraggableTree/utils.ts`
- Modify: `package.json`

**Step 1: Install @dnd-kit packages**

```bash
npm install @dnd-kit/core @dnd-kit/utilities
```

`@dnd-kit/sortable` は不要。useDraggable + useDroppable で3ゾーン判定を自前実装する。

**Step 2: Create types.ts**

```typescript
// src/components/common/DraggableTree/types.ts
export interface TreeNode {
  id: string;
  parentId?: string;
  [key: string]: unknown;
}

export type DropPosition = 'before' | 'inside' | 'after';

export interface DropTarget {
  nodeId: string;
  position: DropPosition;
}

export interface DraggableTreeProps {
  nodes: TreeNode[];
  renderNode: (node: TreeNode, depth: number) => React.ReactNode;
  onMove: (id: string, newParentId: string | undefined, index: number) => void;
  onSelect?: (ids: string[]) => void;
  selectedIds?: string[];
  indentPx?: number;
}
```

**Step 3: Create utils.ts**

```typescript
// src/components/common/DraggableTree/utils.ts
import type { TreeNode } from './types';

export interface FlattenedNode {
  node: TreeNode;
  depth: number;
  /** この親の子の中での順序 */
  indexInParent: number;
}

/**
 * parentId ベースのフラットリストを、ツリー順にソートして depth 付きで返す。
 * 展開中ノードの子のみ含む。
 */
export function flattenTree(
  nodes: TreeNode[],
  expandedIds: Set<string>
): FlattenedNode[] {
  const childrenMap = new Map<string | undefined, TreeNode[]>();
  for (const node of nodes) {
    const key = node.parentId;
    const siblings = childrenMap.get(key);
    if (siblings) {
      siblings.push(node);
    } else {
      childrenMap.set(key, [node]);
    }
  }

  const result: FlattenedNode[] = [];

  function traverse(parentId: string | undefined, depth: number) {
    const children = childrenMap.get(parentId);
    if (!children) return;
    children.forEach((child, index) => {
      result.push({ node: child, depth, indexInParent: index });
      if (expandedIds.has(child.id)) {
        traverse(child.id, depth + 1);
      }
    });
  }

  traverse(undefined, 0);
  return result;
}

/**
 * nodeId が ancestorId の子孫かどうか。循環防止に使う。
 */
export function isDescendant(
  nodes: TreeNode[],
  ancestorId: string,
  nodeId: string
): boolean {
  const nodeMap = new Map(nodes.map((n) => [n.id, n]));
  let current = nodeMap.get(nodeId);
  while (current?.parentId) {
    if (current.parentId === ancestorId) return true;
    current = nodeMap.get(current.parentId);
  }
  return false;
}

/**
 * 指定ノードの子の数を返す。
 */
export function getChildCount(nodes: TreeNode[], parentId: string): number {
  return nodes.filter((n) => n.parentId === parentId).length;
}

/**
 * DropPosition + ターゲットノード情報から、onMove の引数 (newParentId, index) を計算。
 */
export function resolveDropTarget(
  nodes: TreeNode[],
  targetNodeId: string,
  position: 'before' | 'inside' | 'after'
): { newParentId: string | undefined; index: number } {
  const target = nodes.find((n) => n.id === targetNodeId);
  if (!target) return { newParentId: undefined, index: 0 };

  if (position === 'inside') {
    // ターゲットの子として末尾に追加
    const childCount = nodes.filter((n) => n.parentId === targetNodeId).length;
    return { newParentId: targetNodeId, index: childCount };
  }

  // before/after: ターゲットの兄弟として挿入
  const siblings = nodes.filter((n) => n.parentId === target.parentId);
  const targetIndex = siblings.findIndex((n) => n.id === targetNodeId);
  const index = position === 'before' ? targetIndex : targetIndex + 1;
  return { newParentId: target.parentId, index };
}
```

**Step 4: Verify**

```bash
npx tsc --noEmit 2>&1 | grep -i "DraggableTree" | head -5
```

Expected: No errors from our new files (or no output).

**Step 5: Commit**

```bash
git add src/components/common/DraggableTree/ package.json package-lock.json
git commit -m "feat(dnd-tree): install @dnd-kit, add types and utils [T242]"
```

---

### Task 2: DraggableTree + TreeNodeWrapper + DropIndicator

**Files:**
- Create: `src/components/common/DraggableTree/TreeNodeWrapper.tsx`
- Create: `src/components/common/DraggableTree/DropIndicator.tsx`
- Create: `src/components/common/DraggableTree/DraggableTree.tsx`
- Create: `src/components/common/DraggableTree/index.ts`

**Step 1: Create DropIndicator.tsx**

ドロップ位置のビジュアルフィードバック。before/after = 青い水平線、inside = 青枠ハイライト。

```tsx
// src/components/common/DraggableTree/DropIndicator.tsx
'use client';

import type { DropPosition } from './types';

interface DropIndicatorProps {
  position: DropPosition;
  depth: number;
  indentPx: number;
}

export function DropIndicator({ position, depth, indentPx }: DropIndicatorProps) {
  if (position === 'inside') return null; // inside はノード自体をハイライト

  const left = depth * indentPx + 8;

  return (
    <div
      className="pointer-events-none absolute right-0 z-10"
      style={{
        left: `${left}px`,
        ...(position === 'before' ? { top: 0 } : { bottom: 0 }),
        height: '2px',
        backgroundColor: '#3b82f6',
      }}
    />
  );
}
```

**Step 2: Create TreeNodeWrapper.tsx**

各ノードの D&D ラッパー。useDraggable + useDroppable を使い、ポインタ位置から3ゾーン判定。

```tsx
// src/components/common/DraggableTree/TreeNodeWrapper.tsx
'use client';

import { useRef, useCallback } from 'react';
import { useDraggable, useDroppable } from '@dnd-kit/core';
import type { TreeNode, DropPosition } from './types';
import { DropIndicator } from './DropIndicator';

interface TreeNodeWrapperProps {
  node: TreeNode;
  depth: number;
  indentPx: number;
  isSelected: boolean;
  isExpanded: boolean;
  hasChildren: boolean;
  isDragSource: boolean;
  dropPosition: DropPosition | null; // このノードがドロップ先のとき
  onToggleExpand: (id: string) => void;
  onPointerZone: (nodeId: string, position: DropPosition | null) => void;
  renderNode: (node: TreeNode, depth: number) => React.ReactNode;
  onSelect?: (id: string, e: React.MouseEvent) => void;
}

export function TreeNodeWrapper({
  node,
  depth,
  indentPx,
  isSelected,
  isDragSource,
  dropPosition,
  onPointerZone,
  renderNode,
  onSelect,
}: TreeNodeWrapperProps) {
  const nodeRef = useRef<HTMLDivElement>(null);

  const { attributes, listeners, setNodeRef: setDragRef, isDragging } = useDraggable({
    id: node.id,
    data: { node },
  });

  const { setNodeRef: setDropRef, isOver } = useDroppable({
    id: node.id,
    data: { node },
  });

  // ポインタY位置から3ゾーン判定
  const handlePointerMove = useCallback(
    (e: React.PointerEvent) => {
      if (!nodeRef.current) return;
      const rect = nodeRef.current.getBoundingClientRect();
      const relY = (e.clientY - rect.top) / rect.height;
      let position: DropPosition;
      if (relY < 0.25) {
        position = 'before';
      } else if (relY > 0.75) {
        position = 'after';
      } else {
        position = 'inside';
      }
      onPointerZone(node.id, position);
    },
    [node.id, onPointerZone]
  );

  const handlePointerLeave = useCallback(() => {
    onPointerZone(node.id, null);
  }, [node.id, onPointerZone]);

  // 両方の ref を統合
  const setRefs = useCallback(
    (el: HTMLDivElement | null) => {
      nodeRef.current = el;
      setDragRef(el);
      setDropRef(el);
    },
    [setDragRef, setDropRef]
  );

  return (
    <div
      ref={setRefs}
      className="relative"
      style={{
        paddingLeft: `${depth * indentPx}px`,
        opacity: isDragging ? 0.3 : 1,
      }}
      onPointerMove={isOver ? handlePointerMove : undefined}
      onPointerLeave={isOver ? handlePointerLeave : undefined}
      onClick={(e) => onSelect?.(node.id, e)}
      {...attributes}
      {...listeners}
    >
      <div
        className={[
          'flex cursor-grab items-center gap-1 rounded px-2 py-1 text-sm',
          isSelected ? 'bg-accent font-medium' : 'hover:bg-accent/50',
          dropPosition === 'inside' ? 'ring-2 ring-blue-500' : '',
          isDragSource ? 'opacity-30' : '',
        ]
          .filter(Boolean)
          .join(' ')}
      >
        {renderNode(node, depth)}
      </div>
      {dropPosition && dropPosition !== 'inside' && (
        <DropIndicator position={dropPosition} depth={depth} indentPx={indentPx} />
      )}
    </div>
  );
}
```

**Step 3: Create DraggableTree.tsx**

メインコンポーネント。DndContext + フラット化 + 展開/折りたたみ管理。

```tsx
// src/components/common/DraggableTree/DraggableTree.tsx
'use client';

import { useState, useMemo, useCallback } from 'react';
import {
  DndContext,
  DragOverlay,
  PointerSensor,
  useSensor,
  useSensors,
  type DragStartEvent,
  type DragEndEvent,
} from '@dnd-kit/core';
import type { DraggableTreeProps, DropPosition, DropTarget } from './types';
import { flattenTree, isDescendant, resolveDropTarget } from './utils';
import { TreeNodeWrapper } from './TreeNodeWrapper';

const DEFAULT_INDENT_PX = 16;

export function DraggableTree({
  nodes,
  renderNode,
  onMove,
  onSelect,
  selectedIds = [],
  indentPx = DEFAULT_INDENT_PX,
}: DraggableTreeProps) {
  const [expandedIds, setExpandedIds] = useState<Set<string>>(() => {
    // 子を持つノードは初期展開
    const parents = new Set<string>();
    for (const n of nodes) {
      if (n.parentId) parents.add(n.parentId);
    }
    return parents;
  });
  const [activeId, setActiveId] = useState<string | null>(null);
  const [dropTarget, setDropTarget] = useState<DropTarget | null>(null);

  const sensors = useSensors(
    useSensor(PointerSensor, {
      activationConstraint: { distance: 5 },
    })
  );

  const flattened = useMemo(
    () => flattenTree(nodes, expandedIds),
    [nodes, expandedIds]
  );

  const handleToggleExpand = useCallback((id: string) => {
    setExpandedIds((prev) => {
      const next = new Set(prev);
      if (next.has(id)) next.delete(id);
      else next.add(id);
      return next;
    });
  }, []);

  const handleDragStart = useCallback((event: DragStartEvent) => {
    setActiveId(String(event.active.id));
  }, []);

  const handlePointerZone = useCallback(
    (nodeId: string, position: DropPosition | null) => {
      if (!activeId) return;
      if (!position) {
        setDropTarget(null);
        return;
      }
      // 循環防止: 自分自身や子孫へのドロップは無効
      if (nodeId === activeId || isDescendant(nodes, activeId, nodeId)) {
        setDropTarget(null);
        return;
      }
      setDropTarget({ nodeId, position });
    },
    [activeId, nodes]
  );

  const handleDragEnd = useCallback(
    (event: DragEndEvent) => {
      const draggedId = String(event.active.id);

      if (dropTarget) {
        const { newParentId, index } = resolveDropTarget(
          nodes,
          dropTarget.nodeId,
          dropTarget.position
        );
        onMove(draggedId, newParentId, index);

        // 子として追加した場合、親を展開
        if (dropTarget.position === 'inside') {
          setExpandedIds((prev) => new Set(prev).add(dropTarget.nodeId));
        }
      }

      setActiveId(null);
      setDropTarget(null);
    },
    [dropTarget, nodes, onMove]
  );

  const handleDragCancel = useCallback(() => {
    setActiveId(null);
    setDropTarget(null);
  }, []);

  const handleSelect = useCallback(
    (id: string, e: React.MouseEvent) => {
      if (!onSelect) return;
      if (e.metaKey || e.ctrlKey) {
        const newIds = selectedIds.includes(id)
          ? selectedIds.filter((sid) => sid !== id)
          : [...selectedIds, id];
        onSelect(newIds);
      } else {
        onSelect([id]);
      }
    },
    [onSelect, selectedIds]
  );

  const activeNode = activeId ? nodes.find((n) => n.id === activeId) : null;

  return (
    <DndContext
      sensors={sensors}
      onDragStart={handleDragStart}
      onDragEnd={handleDragEnd}
      onDragCancel={handleDragCancel}
    >
      <div className="select-none">
        {flattened.map(({ node, depth }) => {
          const hasChildren = nodes.some((n) => n.parentId === node.id);
          return (
            <TreeNodeWrapper
              key={node.id}
              node={node}
              depth={depth}
              indentPx={indentPx}
              isSelected={selectedIds.includes(node.id)}
              isExpanded={expandedIds.has(node.id)}
              hasChildren={hasChildren}
              isDragSource={activeId === node.id}
              dropPosition={dropTarget?.nodeId === node.id ? dropTarget.position : null}
              onToggleExpand={handleToggleExpand}
              onPointerZone={handlePointerZone}
              renderNode={renderNode}
              onSelect={handleSelect}
            />
          );
        })}
      </div>

      <DragOverlay>
        {activeNode ? (
          <div className="rounded bg-accent px-3 py-1 text-sm shadow-lg opacity-80">
            {renderNode(activeNode, 0)}
          </div>
        ) : null}
      </DragOverlay>
    </DndContext>
  );
}
```

**Step 4: Create index.ts**

```typescript
// src/components/common/DraggableTree/index.ts
export { DraggableTree } from './DraggableTree';
export type { TreeNode, DraggableTreeProps, DropPosition } from './types';
```

**Step 5: Verify**

```bash
npx tsc --noEmit 2>&1 | grep -i "DraggableTree" | head -5
```

**Step 6: Commit**

```bash
git add src/components/common/DraggableTree/
git commit -m "feat(dnd-tree): implement DraggableTree component with 3-zone drop detection [T242]"
```

---

### Task 3: Test page

**Files:**
- Create: `src/app/test/dnd-tree/page.tsx`

**Step 1: Create test page**

```tsx
// src/app/test/dnd-tree/page.tsx
'use client';

import { useState, useRef } from 'react';
import { DraggableTree, type TreeNode } from '@/components/common/DraggableTree';

const INITIAL_NODES: TreeNode[] = [
  { id: 'root1', name: 'Window' },
  { id: 'child1', parentId: 'root1', name: 'Header' },
  { id: 'child2', parentId: 'root1', name: 'Body' },
  { id: 'grandchild1', parentId: 'child2', name: 'Label' },
  { id: 'grandchild2', parentId: 'child2', name: 'Button' },
  { id: 'root2', name: 'HUD' },
  { id: 'child3', parentId: 'root2', name: 'HP Bar' },
  { id: 'child4', parentId: 'root2', name: 'MP Bar' },
  { id: 'child5', parentId: 'root2', name: 'Status Icons' },
  { id: 'grandchild3', parentId: 'child5', name: 'Poison' },
  { id: 'grandchild4', parentId: 'child5', name: 'Sleep' },
  { id: 'root3', name: 'Dialog Box' },
];

interface LogEntry {
  time: string;
  message: string;
}

export default function DndTreeTestPage() {
  const [nodes, setNodes] = useState<TreeNode[]>(INITIAL_NODES);
  const [selectedIds, setSelectedIds] = useState<string[]>([]);
  const [logs, setLogs] = useState<LogEntry[]>([]);
  const logEndRef = useRef<HTMLDivElement>(null);

  const addLog = (msg: string) => {
    const time = new Date().toLocaleTimeString('ja-JP', { hour12: false });
    setLogs((prev) => [...prev.slice(-49), { time, message: msg }]);
  };

  const handleMove = (id: string, newParentId: string | undefined, index: number) => {
    const node = nodes.find((n) => n.id === id);
    const targetName = newParentId
      ? (nodes.find((n) => n.id === newParentId)?.name as string) ?? newParentId
      : '(root)';
    addLog(`Move "${node?.name}" → parent="${targetName}", index=${index}`);

    setNodes((prev) => {
      const updated = prev.map((n) =>
        n.id === id ? { ...n, parentId: newParentId } : n
      );

      // 兄弟間の並び順を更新
      const siblings = updated.filter(
        (n) => n.parentId === newParentId && n.id !== id
      );
      const movedNode = updated.find((n) => n.id === id)!;
      siblings.splice(index, 0, movedNode);

      // siblings の順序を反映して全ノードを再構築
      const nonSiblings = updated.filter(
        (n) => n.parentId !== newParentId || n.id === id ? false : true
      ).filter((n) => n.id !== id);
      // 実際には配列内の順序を調整
      const result: TreeNode[] = [];
      const siblingParent = newParentId;
      for (const n of updated) {
        if (n.id === id) continue;
        if (n.parentId === siblingParent) continue;
        result.push(n);
      }
      // 兄弟を正しい位置に挿入（親の最初の子が出現するインデックス付近）
      // 簡易実装: 非兄弟 + 兄弟の順で結合
      return [...result, ...siblings];
    });
  };

  return (
    <div className="flex h-screen">
      {/* Left: Tree */}
      <div className="w-80 shrink-0 border-r p-4">
        <h2 className="mb-4 text-lg font-bold">DraggableTree Test</h2>
        <div className="rounded border p-2">
          <DraggableTree
            nodes={nodes}
            selectedIds={selectedIds}
            onSelect={setSelectedIds}
            onMove={handleMove}
            renderNode={(node) => (
              <span>{node.name as string}</span>
            )}
          />
        </div>
        <button
          className="mt-4 rounded bg-gray-200 px-3 py-1 text-sm hover:bg-gray-300"
          onClick={() => {
            setNodes(INITIAL_NODES);
            setSelectedIds([]);
            setLogs([]);
            addLog('Reset to initial state');
          }}
        >
          Reset
        </button>
      </div>

      {/* Right: State inspector */}
      <div className="flex flex-1 flex-col overflow-hidden p-4">
        {/* Nodes JSON */}
        <div className="mb-4 flex-1 overflow-auto">
          <h3 className="mb-2 text-sm font-semibold">Current Nodes</h3>
          <pre className="rounded bg-gray-100 p-3 text-xs leading-relaxed">
            {JSON.stringify(
              nodes.map(({ id, parentId, name }) => ({ id, parentId, name })),
              null,
              2
            )}
          </pre>
        </div>

        {/* Logs */}
        <div className="h-48 shrink-0 overflow-auto rounded border p-2">
          <h3 className="mb-1 text-sm font-semibold">Move Log</h3>
          {logs.map((log, i) => (
            <div key={i} className="text-xs text-muted-foreground">
              <span className="mr-2 text-gray-400">{log.time}</span>
              {log.message}
            </div>
          ))}
          <div ref={logEndRef} />
        </div>
      </div>
    </div>
  );
}
```

**Step 2: Verify**

```bash
npx tsc --noEmit 2>&1 | grep -i "dnd-tree\|DraggableTree" | head -5
```

ブラウザで `http://localhost:3000/test/dnd-tree` を開いて動作確認:
1. ノードをドラッグできること
2. ドラッグ中にインジケーター（青線/青枠）が表示されること
3. ドロップで親子関係が変更されること
4. 右側のJSONとログがリアルタイム更新されること
5. 自分自身や子孫へのドロップが無効になること

**Step 3: Commit**

```bash
git add src/app/test/dnd-tree/
git commit -m "feat(dnd-tree): add test page for DraggableTree [T242]"
```

---

### Task 4: 動作確認 + 修正

**Step 1: ブラウザ確認**

`http://localhost:3000/test/dnd-tree` で以下を確認:

| テスト | 期待結果 |
|--------|----------|
| ノードを5px以上ドラッグ | DragOverlay が表示される |
| ノード上部 (1/4) にホバー | 青い水平線が上に表示 |
| ノード中央 (2/4) にホバー | ノードが青枠ハイライト |
| ノード下部 (1/4) にホバー | 青い水平線が下に表示 |
| 子孫ノードにドロップ | 何も起きない（循環防止） |
| 中央にドロップ | ノードが子として追加、親が展開 |
| 上部にドロップ | ノードが兄弟として前に挿入 |
| 下部にドロップ | ノードが兄弟として後に挿入 |
| ルートの空エリアにドロップ | ルートに移動 |
| 右側 JSON | リアルタイム更新 |
| ログ | onMove 呼び出しが記録される |

**Step 2: 問題があれば修正、なければ最終コミット**

```bash
npx tsc --noEmit
```
